node_modules/
dist/
.env
*.log
.DS_Store

# Arquivos CSV
*.csv

# Base de dados CNPJ (dados de atualização — não versionar, vão pra VPS via rsync/scp)
dados_cnpj/

# Base CNEFE/IBGE (geocodificação — só downloads, regeneráveis)
dados_cnefe/

# cobertura de testes
coverage/

# backups e config rclone de produção
backups/
rclone/
